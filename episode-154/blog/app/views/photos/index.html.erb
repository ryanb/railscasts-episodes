<% title "Photos" %>

<% for photo in @photos %>
  <div class="photo">
    <%= image_tag photo.image.url(:original) %>
    <p><%=h photo.name %></p>
  </div>
<% end %>

<p><%= link_to "New Photo", new_photo_path %></p>
